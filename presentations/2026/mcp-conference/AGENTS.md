
- Use Markdown links for images and other content (example: `![Image](https://link.to.image)`). Do this for video as well
- We strictly use `uv` in this environment. The Hugging Face CLI is installed (`hf`) and already authenticated.
- When generation any Python code, always use the PEP 783 style imports

